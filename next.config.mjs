/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: [
            'images.pexels.com'// أضف هذا الدومين الجديد هنا
          ],
       
      },
};

export default nextConfig;

